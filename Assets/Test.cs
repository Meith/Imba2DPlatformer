using UnityEngine;
using System.Collections;

public class Test : MonoBehaviour {

	// Use this for initialization
	void Start () {
      //  this.GetComponent<Rigidbody>().AddForce( Vector3.down) * 3000f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
